dependencies {
  compile project(':Lib')
  compile project(':Platform')
  compile project(':GraphLayout')
  testCompile testDependencies
}

sourceSets {
  test {
    java {
      srcDir 'test'
    }
  }
}