dependencies {
  compile project(':Platform')
  testCompile parent.ext.testDependencies
}

sourceSets {
  main {
    resources {
      srcDir 'resources'
    }
  }
  test {
    java {
      srcDir 'test'
    }
  }
}