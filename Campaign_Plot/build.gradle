dependencies {
  compile project(':Platform')
  testCompile testDependencies
}

sourceSets {
  main {
    resources {
      srcDir 'resources'
    }
  }
  test {
    java {
      srcDir 'test'
    }
  }
}