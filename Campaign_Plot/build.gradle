dependencies {
  compile project(':Platform')
  testCompile group: 'junit', name: 'junit', version: '4.+'
}

sourceSets {
  main {
    resources {
      srcDir 'resources'
    }
  }
  test {
    java {
      srcDir 'test'
    }
  }
}