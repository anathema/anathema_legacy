dependencies {
  compile project(':Platform')
  //TODO (Swing->FX): Separate Namegenerator backend and NG-module so this doesn't introduce FX-dependencies into Character_Main
  compile project(':Platform_FX') {
    transitive = false
  }
  compile project(':Platform_Swing') {
    transitive = false
  }
}