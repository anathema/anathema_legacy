dependencies {
  compile project(':Character_Library')
  compile project(':Character_Main')
  compile project(':Character_Reporting')
}

sourceSets {
  main {
    resources {
      srcDir 'resources'
    }
  }
}